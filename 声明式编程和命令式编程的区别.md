# 命令式编程和声明式编程的区别
- 命令式编程是一种基于操作、命令和语句的编程范式，需要指定每个操作的具体步骤和指令顺序，以及如何处理每个判断条件，语句的执行结果会影响后续语句的执行情况
- 声明式编程是一种以描述目标、规则和结果为核心的编程范式，开发者只需要关注描述“要什么”，而不是传统上去控制程序执行的每一步，系统根据描述自动完成细节的实现

## 命令式编程 Imperative Programming
- 告诉计算机一步一步怎么做，程序由一系列命令组成，重点在执行过程、状态变化、控制流程（指令比较细，比较容易理解，顺序执行，比较容易控制）
- 强调步骤和过程，侧重于 “How 怎么做”，描述达成目标的步骤和操作顺序，需要比较精确地描述过程（包括数据如何被处理、存储和修改）
- 也称为过程式编程
```java
public static void main(String[] args) {
    int a = 1;
    int b = 2;
    int sum = a + b;
    System.out.println("sum = "+sum);
    a = 3;
    b = 4;
    System.out.println("sum = "+sum);
    //------------ 打印 ---------
    sum = 3
    sum = 3 //第二次输出还是 3，因为缺少了再次执行相加命令的指令，而在声明式编程中 sum 就可以引申成一个两数相加的规则（定义一种关系），当数据发生变化时，他们应该自动完成相加而不是一定非要加上相加的指令
}
```
## 声明式编程 Declarative Programming
- 单纯的描述想要什么，计算规则，而不是如何实现，具体的计算步骤和状态维护，交给底层系统框架自行去处理（人为干预少了，就不会丢三落四了，大大降低了出错的可能性）
- 声明描述 UI 在不同状态下“应该长什么样”，侧重于“What 做什么”

## 响应式编程和函数式编程
- 在上述基础思想上衍生出的更高级、更具体的编程风格，比如响应式编程、函数式编程等
- 响应式编程：被视为一种面向数据流和变化传播的编程范式，通常被视为声明式编程的一种
- 函数式编程：是一种编程范式，它将计算机运算视为函数式映射，并避免使用可变的状态和副作用

## 响应式编程 Reactive Programming
- 强调数据流、事件流和自动变化传播（关注异步数据流的构建，当数据、事件或状态发生变化时，相关逻辑自动响应）
- 通过构建好事物与事物之间的关系，使得程序能够更直观地表达静态或动态的数据流，并自动将变化的值通过数据流进行传播
- 事件驱动：一切皆是流（用户的点击、网络请求、传感器数据、甚至变量的变化）
- 观察者模式：常基于观察者模式或发布订阅模式实现，通过订阅数据流来接收变化通知，实现对象订阅其他对象的变化以作出反应
- 常见实现库包括 RxJava、LiveData、Kotlin Flow 等

## 函数式编程 Functional Programming
- 强调函数是一等公民：函数式编程最重要的特点，函数可以作为参数传递给其他函数，也可以作为返回值返回，并且函数本身也可以赋值给变量
- 将计算过程视为数学上的函数计算，极力避免使用“程序状态”和“可变对象”
- 纯函数：无副作用、无状态，相同的输入永远会得到相同的输出，函数的计算结果不依赖于外部状态（避免了共享可变状态，不使用易变的全局变量，不对变量进行读写）
- 不可变数据：数据一旦创建，就不能被修改，只能通过创建新的数据来实现修改


